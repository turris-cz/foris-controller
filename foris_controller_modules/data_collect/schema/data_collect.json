{
    "oneOf": [
        {
            "description": "Get information whether specified user has registered the router",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get_registered"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "email": {"type": "string"},
                        "language": { "$def": "#/definitions/iso2country" }
                    },
                    "additionalProperties": false,
                    "required": ["email", "language"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Reply whether specified user has registered the router",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["reply"]},
                "action": {"enum": ["get_registered"]},
                "data": {
                    "oneOf": [
                        {
                            "type": "object",
                            "properties": {
                                "status": {"enum": ["unknown", "owned", "not_found"]}
                            },
                            "additionalProperties": false,
                            "required": ["status"]
                        },
                        {
                            "type": "object",
                            "properties": {
                                "status": {"enum": ["foreign", "free"]},
                                "url": {"type": "string"},
                                "registration_number": {
                                    "type": "string", "pattern": "^[a-zA-Z0-9]{16}"
                                }
                            },
                            "additionalProperties": false,
                            "required": ["status", "url", "registration_number"]
                        }
                    ]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Request to get information whether about data collect",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        },
        {
            "description": "Response to get information whether about data collect",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["reply"]},
                "action": {"enum": ["get"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "agreed": {"type": "boolean"}
                    },
                    "additionalProperties": false,
                    "required": ["agreed"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Request to update configuration of data collect",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["set"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "agreed": {"type": "boolean"}
                    },
                    "additionalProperties": false,
                    "required": ["agreed"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Reply to update configuration of data collect",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["reply"]},
                "action": {"enum": ["set"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "result": {"type": "boolean"}
                    },
                    "additionalProperties": false,
                    "required": ["result"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        },
        {
            "description": "Notification that configuration of data collect changed",
            "properties": {
                "module": {"enum": ["data_collect"]},
                "kind": {"enum": ["notification"]},
                "action": {"enum": ["set"]},
                "data": {
                    "type": "object",
                    "properties": {
                        "agreed": {"type": "boolean"}
                    },
                    "additionalProperties": false,
                    "required": ["agreed"]
                }
            },
            "additionalProperties": false,
            "required": ["data"]
        }
    ]
}
